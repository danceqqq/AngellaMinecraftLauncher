# Minecraft Hardcore Launcher

Кастомный лаунчер для нашего хардкор сервера Minecraft на Electron. Вся нужная фигня в одном месте - профили, запуск игры с Fabric, интерактивная карта сервера и интеграция с Discord ботом.

## 📸 Скриншоты

Производительность :
Before
<img width="1280" height="737" alt="image" src="https://github.com/user-attachments/assets/4c285fde-1c56-4ede-b561-027af22ce95c" />

After :
<img width="1280" height="742" alt="image" src="https://github.com/user-attachments/assets/1506df93-eb65-4bee-9f18-d5a9d730cc84" />


Before :
![photo_2025-12-04_14-09-21](https://github.com/user-attachments/assets/3db6ef8d-a681-467d-a3c9-8a7b7ce0a2a5)
After :
<img width="1580" height="922" alt="{10037B80-E8F6-4102-AA23-1BE5A74121F8}" src="https://github.com/user-attachments/assets/4e8e68b3-326b-4460-a0da-e335663ff322" />




## Описание проекта

Делали этот лаунчер специально под наш хардкор сервер. Идея простая - чтобы игроки не парились с настройками и могли быстро запускать игру, смотреть кто онлайн, и вообще все было удобно. В общем, попытка сделать нормальный лаунчер вместо стандартного от Mojang.

Лаунчер состоит из нескольких компонентов:
- **Electron приложение** - основной интерфейс лаунчера
- **Серверный мод (launcherapi)** - предоставляет HTTP API для получения данных о игроках
- **Клиентский мод (launcherclient)** - определяет, что игрок использует наш лаунчер
- **Discord бот (angella)** - отправляет уведомления в Discord о событиях на сервере

## Технологический стек

### Основное приложение (Electron)

Лаунчер на Electron - так проще делать интерфейс, да и работает как обычное приложение. Вот что используем:

```json
{
  "dependencies": {
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "^24.6.4"
  }
}
```

**Что под капотом:**
- Electron 27.0.0 - чтобы сделать десктопное приложение из веб-технологий
- Node.js - для работы с файлами и процессами
- Vanilla JavaScript - без лишних фреймворков, чтобы не тормозило
- HTML5/CSS3 - обычный веб-интерфейс

### Серверный мод (Fabric)

Серверный мод на Java + Fabric API. Поднимает простой HTTP сервер на порту 30761, который отдает информацию о игроках в JSON формате.

**Основные зависимости (build.gradle):**

```gradle
dependencies {
    // Minecraft 1.21.8
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    mappings "net.fabricmc:yarn:${project.yarn_mappings}:v2"
    modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"
    
    // Fabric API
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"
    
    // Gson для работы с JSON
    implementation 'com.google.code.gson:gson:2.10.1'
    include 'com.google.code.gson:gson:2.10.1'
}
```

**Основные классы:**
- `LauncherApiMod` - главный класс, запускает HTTP сервер
- `HttpApiServer` - сам HTTP сервер на обычных Java Sockets (без лишних зависимостей)
- `PlayerJoinHandler` - ловит подключения игроков и проверяет, используют ли они наш лаунчер
- `PlayerInfo` - просто структура данных с информацией об игроке (достижения, время игры и т.д.)

### Клиентский мод (Fabric)

Клиентский мод - простая штука. Его задача одна: при подключении к серверу отправить сигнал "я из лаунчера", чтобы сервер это понял.

**Зависимости:**

```gradle
dependencies {
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    mappings "net.fabricmc:yarn:${project.yarn_mappings}:v2"
    modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"
}
```

**Как работает:**

Мод через reflection отправляет пакет по Fabric Networking API когда игрок подключается. Сервер получает этот пакет и ставит флаг `fromLauncher` - так мы понимаем, что игрок использует наш лаунчер.

### Discord бот

Discord бот тоже на Java, используем JDA 5.1.1 для работы с Discord API.

**Основные зависимости:**

```gradle
dependencies {
    // JDA для работы с Discord API
    implementation('net.dv8tion:JDA:5.1.1') {
        exclude module: 'opus-java'
    }
    
    // HTTP клиент для JDA
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.squareup.okio:okio:3.9.1'
    
    // Дополнительные библиотеки
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.17.1'
    implementation 'com.google.code.gson:gson:2.10.1'
}
```

**Что умеет:**
- Пишет красивые embed сообщения когда кто-то заходит/выходит
- Показывает статистику игрока (сколько времени на сервере, сколько достижений)
- Определяет, использует ли игрок наш лаунчер (и пишет об этом в сообщении)
- Интегрирован с верификацией через Discord

## Установка и настройка

### Требования

Что нужно для работы:
- Windows 10/11 (на других ОС не тестировали, но должно работать)
- Node.js 16 или новее
- Java 21 (чтобы запускать Minecraft и собирать моды)
- Git (если клонируешь репозиторий, но можно и архивом скачать)

### Установка зависимостей

Сначала ставим зависимости:

```bash
npm install
```

Это скачает Electron и все остальное в `node_modules`.

### Сборка модов

Для сборки серверного мода:

```bash
cd ServerMods
gradlew.bat build
```

Готовый мод будет в `ServerMods/build/libs/launcherapi-1.0.0.jar`

Для сборки клиентского мода:

```bash
cd ClientMods
gradlew.bat build
```

Готовый мод будет в `ClientMods/build/libs/launcherclient-1.0.0.jar`

Для сборки Discord бота:

```bash
cd DiscordMinecraft
gradlew.bat build
```

Готовый бот будет в `DiscordMinecraft/build/libs/angella-1.0.0.jar`

### Настройка сервера

1. Установите серверный мод `launcherapi-1.0.0.jar` в папку `mods/` вашего сервера
2. Установите Discord бот `angella-1.0.0.jar` в папку `mods/` сервера
3. Настройте конфигурацию бота в файле `config/angella.json` на сервере:

```json
{
  "botToken": "YOUR_DISCORD_BOT_TOKEN",
  "technicalChannelId": 1256170139318091807,
  "gameChannelId": 1444857215172345886,
  "enabled": true,
  "sendPlayerJoin": true,
  "sendPlayerLeave": true,
  "sendPlayerDeath": true,
  "sendAdvancements": true
}
```

### Настройка лаунчера

Лаунчер сам скопирует клиентский мод в нужную папку когда запустишь игру. Ничего настраивать не нужно.

Основные пути, которые использует лаунчер:

```javascript
// Путь к данным лаунчера
const LAUNCHER_PATH = path.join(process.env.APPDATA, '.angelauncher');

// Путь к версиям Minecraft
const VERSIONS_PATH = path.join(LAUNCHER_PATH, 'versions');

// Путь к модам
const MODS_PATH = path.join(VERSION_PATH, 'mods');
```

## Использование

### Создание профиля

При первом запуске нужно создать профиль:

1. Жми "Создать профиль"
2. Придумай название (типа "Мой основной профиль" или как хочешь)
3. Введи свой Minecraft ник
4. Сохрани

Профили лежат в JSON файле `profiles.json` в папке данных Electron (обычно в AppData).

### Запуск игры

1. Выбери профиль из списка
2. Если нужно, покрути настройки справа (RAM, полноэкранный режим и т.д.)
3. Жми "Запустить игру"

Лаунчер сам:
- Проверит, есть ли Fabric Loader
- Скачает библиотеки если их нет
- Скопирует клиентский мод в папку модов
- Запустит игру с нужными параметрами

### Настройки запуска

В панели настроек (справа, рядом с картой) можно настроить:

- **Оперативная память (RAM)** - от 1GB до 16GB (или сколько у тебя есть)
- **Минимальная RAM** - от 512MB до 4GB
- **Полноэкранный режим** - если хочешь играть на весь экран
- **Быстрый запуск** - сразу подключается к серверу, не нужно вводить IP
- **Дополнительные аргументы Java** - для тех, кто знает что делает

Настройки сохраняются автоматически, ничего не потеряется.

### Просмотр карты

Интерактивная карта BlueMap встроена прямо в лаунчер. Она справа, загружается автоматически когда открываешь лаунчер. Карта тянется с сервера `http://213.171.18.211:30031/`.

## Архитектура проекта

### Структура файлов

```
LauncherMinecraft/
├── main.js                 # Основной процесс Electron (IPC, запуск игры)
├── renderer.js             # Логика интерфейса (UI, профили, события)
├── index.html              # HTML структура интерфейса
├── styles.css              # Стили интерфейса
├── package.json            # Конфигурация Node.js проекта
│
├── ServerMods/             # Серверный мод
│   ├── src/main/java/com/launcher/api/
│   │   ├── LauncherApiMod.java      # Точка входа мода
│   │   ├── HttpApiServer.java       # HTTP сервер
│   │   ├── PlayerJoinHandler.java   # Обработка подключений
│   │   └── model/PlayerInfo.java    # Модель данных игрока
│   └── build.gradle                 # Конфигурация сборки
│
├── ClientMods/             # Клиентский мод
│   ├── src/main/java/com/launcher/client/
│   │   └── LauncherClientMod.java   # Клиентский мод
│   └── build.gradle
│
└── DiscordMinecraft/       # Discord бот
    ├── src/main/java/com/angella/
    │   ├── AngellaMod.java          # Точка входа мода
    │   ├── discord/
    │   │   ├── DiscordBot.java      # Основной класс бота
    │   │   └── EmbedBuilder.java    # Построение embed сообщений
    │   └── config/AngellaConfig.java # Конфигурация
    └── build.gradle
```

### Как все общается между собой

**Лаунчер ↔ Серверный мод:**
- Лаунчер просто делает HTTP запросы к `http://213.171.18.211:30761/api/players`
- Сервер отдает JSON с информацией о игроках

**Клиентский мод ↔ Серверный мод:**
- Клиентский мод при подключении отправляет пакет "я из лаунчера"
- Сервер получает пакет и ставит флаг `fromLauncher`

**Серверный мод ↔ Discord бот:**
- Бот через reflection смотрит флаг `fromLauncher` в `PlayerJoinHandler`
- Если флаг есть - пишет в Discord что игрок использует лаунчер

### IPC коммуникация в Electron

Лаунчер использует Electron IPC чтобы обмениваться данными между главным процессом (main.js) и интерфейсом (renderer.js):

```javascript
// В renderer.js (веб-страница)
const profiles = await ipcRenderer.invoke('get-profiles');
await ipcRenderer.invoke('launch-game', { profile, settings });

// В main.js (главный процесс)
ipcMain.handle('get-profiles', async () => {
    // Чтение файла profiles.json
});

ipcMain.handle('launch-game', async (event, data) => {
    // Запуск процесса Minecraft
});
```

## API серверного мода

Серверный мод поднимает простой HTTP API на порту 30761. Ничего сложного, просто JSON отдает.

### GET /api/players

Возвращает список всех онлайн игроков.

**Пример запроса:**
```bash
curl http://213.171.18.211:30761/api/players
```

**Пример ответа:**
```json
{
  "success": true,
  "online": 2,
  "max": 20,
  "players": [
    {
      "name": "ASSlover",
      "uuid": "fd952715-538d-3844-b614-71ba8d053b66",
      "headUrl": "https://mc-heads.net/avatar/.../32",
      "online": true,
      "achievements": 313,
      "fromLauncher": true,
      "serverPlayTime": 161834
    }
  ]
}
```

**Поля ответа:**
- `name` - имя игрока
- `uuid` - UUID игрока
- `headUrl` - URL аватара игрока
- `online` - статус онлайн
- `achievements` - количество полученных достижений (только с display)
- `fromLauncher` - флаг использования кастомного лаунчера
- `serverPlayTime` - время на сервере в секундах

## Особенности реализации

### Определение использования лаунчера

Серверный мод определяет использование лаунчера двумя способами:

1. **Пакет handshake** - клиентский мод отправляет пакет когда подключается
2. **Reflection fallback** - если пакет не пришел (что-то пошло не так), через reflection проверяем список модов клиента

Код проверки в `PlayerJoinHandler.java`:

```java
// Регистрируем обработчик пакета от клиента
ServerPlayNetworking.registerGlobalReceiver(LAUNCHER_HANDSHAKE, (payload, context) -> {
    ServerPlayerEntity player = context.player();
    context.server().execute(() -> {
        setLauncherFlag(player, true);
        LauncherApiMod.LOGGER.info("Player {} sent launcher handshake", player.getName().getString());
    });
});
```

### Подсчет достижений

Достижения считаем только те, у которых есть display (те что показываются в игре). Так же как и в Discord боте - чтобы не засорять статистику техническими достижениями:

```java
private static int getAchievementsCount(ServerPlayerEntity player) {
    var advancementManager = server.getAdvancementLoader();
    int count = 0;
    for (var advancement : advancementManager.getAdvancements()) {
        // Считаем только достижения с display
        if (advancement.value().display().isPresent()) {
            var progress = advancementTracker.getProgress(advancement);
            if (progress != null && progress.isDone()) {
                count++;
            }
        }
    }
    return count;
}
```

### Сохранение времени игры

Время игры сохраняется в профиле и накапливается между сессиями. То есть если ты играл час сегодня и час завтра - будет 2 часа:

```javascript
// При запуске игры сохраняем начальное время
const initialPlayTime = currentProfile.playTime || 0;

// Во время игры обновляем время
gamePlayTimeInterval = setInterval(() => {
    if (isGameRunning && currentProfile) {
        gamePlayTime++;
        currentProfile.playTime = initialPlayTime + gamePlayTime;
        // Сохраняем каждые 60 секунд
        if (gamePlayTime % 60 === 0) {
            saveProfiles();
        }
    }
}, 1000);
```

## Разработка

### Запуск в режиме разработки

```bash
npm start
```

Это запустит лаунчер в режиме разработки с открытыми DevTools (F12).

### Сборка для распространения

Для создания установщика Windows:

```bash
npm run build-win
```

Установщик появится в папке `dist/`.

### Отладка

Лаунчер пишет подробные логи. Смотреть их можно:
- В Electron DevTools (F12) - там логи интерфейса (renderer)
- В терминале где запускал - там логи главного процесса (main)

Примеры логов:
```
[Main] Launch game called with profile: {...}
[Main] Launch settings: {maxRam: '4G', minRam: '2G', ...}
[Renderer] Received profile update data: {...}
```

## Известные ограничения

- Лаунчер работает только на Windows (можно адаптировать для других ОС)
- Требуется Java 21 для сборки модов
- BlueMap карта должна быть доступна по указанному адресу
- Discord бот требует настройки токена в конфигурации

## История разработки (Changelog)

### Версия 1.1.0 (Текущая версия) - Профессиональные улучшения

#### 🎨 Графические и UI улучшения

**Раскрываемая карта и настройки**
- Карта BlueMap при наведении немного раскрывается (peek эффект) - видно что внутри
- При клике на заголовок карта полностью раскрывается
- То же самое с настройками - навел - видно что там, кликнул - полностью открылось
- Кликнешь в другое место - все сворачивается обратно
- Анимации плавные, с cubic-bezier - выглядит нормально

Реализация в `styles.css`:
```css
.expandable {
    max-height: 200px;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.expandable.peek {
    max-height: 300px;
    border-color: var(--border-glow);
    box-shadow: var(--shadow-lg), 0 0 20px rgba(0, 212, 255, 0.2);
}

.expandable.expanded {
    max-height: 800px;
    border-color: var(--primary-color);
    box-shadow: var(--shadow-lg), 0 0 30px rgba(0, 212, 255, 0.4);
    z-index: 10;
}
```

**Кастомный скроллбар**
- Сделали свой скроллбар в стиле лаунчера (градиент голубой → фиолетовый)
- При наведении меняет цвет - выглядит приятно
- Работает везде где есть списки (профили, игроки, настройки)

```css
::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, var(--primary-color), var(--secondary-color));
    border-radius: 10px;
    transition: var(--transition-fast);
}

::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(180deg, var(--primary-light), var(--primary-color));
}
```

**Бегущая строка игроков**
- Онлайн игроки показываются в виде бегущей строки под заголовком
- Обновляется сама каждые 30 секунд (кнопку "Обновить" убрали)
- Показывает головы игроков и бота
- Анимация зацикленная - игроки прокручиваются бесконечно
- Компактно, чтобы не занимало много места

#### ⚙️ Технические улучшения

**Автоопределение RAM и ползунок**
- Лаунчер сам определяет сколько у тебя RAM
- Ползунок кастомный, с градиентом - выглядит нормально
- Показывает сколько доступно памяти
- Можно выбрать от 1GB до 32GB (или сколько у тебя есть)

Реализация в `main.js`:
```javascript
ipcMain.handle('get-system-ram', async () => {
    const os = require('os');
    const totalRAM = os.totalmem();
    const totalRAMGB = Math.floor(totalRAM / (1024 * 1024 * 1024));
    return totalRAMGB;
});
```

**Fabric-совместимые фичи**
- Режим производительности: включает G1GC сборщик мусора (должно помочь с FPS)
- Режим отладки: для тех кто делает моды - включает debug логи
- Оптимизированные Java аргументы - должно работать быстрее

```javascript
if (performanceModeCheckbox?.checked) {
    const g1gcArgs = '-XX:+UseG1GC -XX:+ParallelRefProcEnabled -XX:MaxGCPauseMillis=200';
    javaArgs = javaArgs ? `${g1gcArgs} ${javaArgs}` : g1gcArgs;
}

if (debugModeCheckbox?.checked) {
    const debugArgs = '-Dfabric.development=true -Dfabric.log.level=debug';
    javaArgs = javaArgs ? `${debugArgs} ${javaArgs}` : debugArgs;
}
```

**Автообновление аватара бота из Discord API**
- Аватар бота берется из Discord API (ID: 1339807926377775182)
- Кэшируется на 5 минут чтобы не спамить запросами
- Если API недоступен - использует локальный файл
- Timeout 3 секунды - если не ответил быстро, берем локальный

```javascript
async function getDiscordBotAvatar() {
    const BOT_ID = '1339807926377775182';
    
    // Проверяем кэш
    if (botAvatarCache && (Date.now() - botAvatarCacheTime) < BOT_AVATAR_CACHE_DURATION) {
        return botAvatarCache;
    }
    
    // Получаем из Discord API
    const response = await https.get(`https://discord.com/api/v10/users/${BOT_ID}`);
    if (response.avatar) {
        return `https://cdn.discordapp.com/avatars/${BOT_ID}/${response.avatar}.png?size=128`;
    }
    
    // Fallback на локальный файл
    return localAvatarPath;
}
```

**Исправление сохранения времени игры**
- Время теперь правильно накапливается между сессиями
- Исправили баг когда время обнулялось
- Сохраняется каждые 60 секунд (на случай если лаунчер закроется)
- При выходе из игры финально сохраняется

```javascript
// При запуске игры
initialPlayTime = currentProfile.playTime || 0;

// Во время игры
gamePlayTimeInterval = setInterval(() => {
    gamePlayTime++;
    currentProfile.playTime = initialPlayTime + gamePlayTime;
    if (gamePlayTime % 60 === 0) {
        saveProfiles();
    }
}, 1000);
```

#### 🐛 Исправления

**Исправление запуска Minecraft**
- Убрали проблемный мод `fastjoin-1.0.0.jar` который крашил игру
- Теперь фильтруем моды при добавлении в classpath
- Игра запускается нормально, без крашей

**Исправление загрузки бота**
- Пофиксили синхронную функцию `addDiscordBotToListSync`
- Убрали ошибку "An object could not be cloned" (была из-за Promise)
- Бот теперь правильно отображается в бегущей строке

### Версия 1.0.0

#### Основные функции

**Управление профилями**
- Можно создавать, редактировать и удалять профили
- Профили сохраняются в JSON (можно даже руками править если нужно)
- Показывает голову игрока в выбранном профиле
- UUID получается автоматически по никнейму

**Отображение статистики**
- Время игры в лаунчере (накапливается, не сбрасывается)
- Время на сервере (синхронизируется с сервером когда заходишь)
- Количество достижений (только те что с display)
- Время показывается в нормальном виде (часы/минуты, не просто секунды)

Реализация подсчета времени в `renderer.js`:

```javascript
// При запуске игры сохраняем начальное время
const initialPlayTime = currentProfile.playTime || 0;

// Во время игры обновляем время каждую секунду
gamePlayTimeInterval = setInterval(() => {
    if (isGameRunning && currentProfile) {
        gamePlayTime++;
        currentProfile.playTime = initialPlayTime + gamePlayTime;
        // Сохраняем каждые 60 секунд для предотвращения потери данных
        if (gamePlayTime % 60 === 0) {
            saveProfiles();
        }
    }
}, 1000);
```

**Интеграция с сервером**
- HTTP API чтобы получить список кто онлайн
- Список обновляется сам каждые 30 секунд
- Показываем Discord бота "Angella" в списке игроков (с его аватаром)
- Получаем данные о достижениях и времени на сервере

**Интерактивная карта BlueMap**
- Карта сервера встроена справа
- При наведении немного раскрывается, при клике - полностью
- Загружается сама с сервера

**Настройки запуска**
- Можно выбрать сколько RAM выделить (1GB - 16GB, или сколько есть)
- Минимальная RAM (512MB - 4GB)
- Полноэкранный режим если хочешь
- Быстрый запуск - сразу на сервер без ввода IP
- Дополнительные аргументы Java для тех кто знает что делает

Пример применения настроек в `main.js`:

```javascript
const args = [
    `-Xmx${settings.maxMemory || '2G'}`,
    `-Xms${settings.minMemory || '1G'}`,
    // ... другие аргументы
];

if (settings.fullscreen) {
    args.push('--fullscreen');
}
if (settings.quickPlay) {
    args.push('--quickPlaySingleplayer', '213.171.18.211:30081');
}
if (settings.javaArgs) {
    args.push(...settings.javaArgs.split(' ').filter(arg => arg.trim() !== ''));
}
```

#### Интеграция с Discord ботом

**Определение использования лаунчера**
- Клиентский мод при подключении отправляет сигнал "я из лаунчера"
- Сервер получает сигнал и понимает что игрок использует наш лаунчер
- Discord бот видит это и пишет в сообщении что игрок использует лаунчер

Реализация определения лаунчера в `PlayerJoinHandler.java`:

```java
// Проверка с задержкой для полной загрузки модов клиента
server.executeAfter(2 * 20, () -> {
    boolean hasLauncherMod = false;
    try {
        if (handler instanceof ServerPlayNetworkHandler) {
            ServerPlayNetworkHandler networkHandler = (ServerPlayNetworkHandler) handler;
            // Поиск мода launcherclient в списке модов клиента через reflection
            hasLauncherMod = findModListAndCheck(networkHandler, "launcherclient");
        }
    } catch (Exception e) {
        LauncherApiMod.LOGGER.warn("Failed to check launcher mod", e);
    }
    setLauncherFlag(player, hasLauncherMod);
});
```

**Адаптация сообщений Discord**
- Если игрок использует лаунчер - бот пишет "Подключился! Используя Лаунчер!"
- В embed сообщениях показывается время на сервере и достижения
- Поддерживаются все варианты приветствий

#### Исправленные проблемы

**UI и интерфейс**
- Убрали горизонтальную прокрутку, оставили только вертикальную
- Пофиксили отображение профилей когда выбираешь
- Исправили анимацию карты (теперь нормально открывается)
- Вернули тег "beta.png" рядом с игроками
- Исправили удаление профилей (теперь работает)

**Функциональность**
- Пофиксили сохранение времени игры (больше не обнуляется)
- Исправили импорт времени с сервера
- Бот теперь правильно показывается в списке игроков
- Добавили кнопку установки Fabric (на случай если его нет)
- Исправили ошибку "No handler registered for 'is-game-running'"

**Подсчет достижений**
- Исправили подсчет достижений (считаем только те что с display, как в игре)
- Теперь синхронизировано с логикой Discord бота

Исправление в `PlayerInfo.java`:

```java
private static int getAchievementsCount(ServerPlayerEntity player) {
    var advancementManager = server.getAdvancementLoader();
    int count = 0;
    for (var advancement : advancementManager.getAdvancements()) {
        // Считаем только достижения с display (отображаются в игре)
        if (advancement.value().display().isPresent()) {
            var progress = advancementTracker.getProgress(advancement);
            if (progress != null && progress.isDone()) {
                count++;
            }
        }
    }
    return count;
}
```

#### Технические улучшения

**Обработка нативных библиотек**
- LWJGL нативные библиотеки извлекаются автоматически
- Пути настроены правильно для работы с OpenGL

**IPC коммуникация**
- Все нужные IPC handlers реализованы
- Данные между процессами передаются нормально

**Автоматическое копирование модов**
- Клиентский мод сам копируется в папку модов когда запускаешь игру

Код копирования в `main.js`:

```javascript
// Копируем клиентский мод в папку mods
const clientModSourcePath = path.join(app.getAppPath(), 'ClientMods', 'build', 'libs', 'launcherclient-1.0.0.jar');
const clientModDestPath = path.join(MODS_PATH, 'launcherclient-1.0.0.jar');

if (fsSync.existsSync(clientModSourcePath)) {
    try {
        await fs.copyFile(clientModSourcePath, clientModDestPath);
        console.log('[Main] Successfully copied client mod to:', clientModDestPath);
    } catch (error) {
        console.error('[Main] Failed to copy client mod:', error);
    }
}
```

**WebGL2 поддержка**
- Включили WebGL2 для BlueMap карты (чтобы работала нормально)
- Оптимизировали iframe чтобы не тормозило

### Этапы разработки

**Этап 1: Базовая функциональность**
- Сделали Electron приложение
- Добавили управление профилями
- Научили запускать Minecraft через лаунчер
- Интегрировали Fabric Loader

**Этап 2: Интеграция с сервером**
- Написали серверный мод (launcherapi)
- Сделали HTTP API чтобы получать данные о игроках
- Добавили отображение онлайн игроков в лаунчере
- Интегрировали с SkinRestorer чтобы получать головы игроков

**Этап 3: Discord интеграция**
- Написали Discord бота (angella)
- Бот отправляет embed сообщения о событиях на сервере
- Научили определять использование кастомного лаунчера
- Бот адаптирует сообщения в зависимости от лаунчера

**Этап 4: Клиентский мод**
- Написали клиентский мод (launcherclient)
- Мод отправляет сигнал серверу что используется лаунчер
- Мод автоматически копируется при запуске игры

**Этап 5: Улучшения UI**
- Добавили панель настроек запуска
- Улучшили отображение статистики
- Пофиксили проблемы с прокруткой и отображением
- Оптимизировали интерфейс

**Этап 6: Исправления и оптимизация**
- Исправили баги с сохранением данных
- Оптимизировали производительность
- Улучшили обработку ошибок
- Доделали интерфейс

### Используемые технологии и библиотеки

**Frontend (Electron)**
- Electron 27.0.0
- Node.js
- Vanilla JavaScript (без фреймворков)
- HTML5/CSS3
- adm-zip 0.5.16 (для работы с архивами)

**Backend (Серверный мод)**
- Java 21
- Fabric Loader 0.17.3
- Fabric API
- Gson 2.10.1 (для JSON)

**Backend (Клиентский мод)**
- Java 21
- Fabric Loader 0.17.3
- Fabric API

**Discord бот**
- Java 21
- JDA 5.1.1 (Java Discord API)
- OkHttp 4.12.0 (HTTP клиент)
- OkIO 3.9.1
- Jackson 2.17.1 (JSON)
- Gson 2.10.1 (конфигурация)

### Известные ограничения и недочеты

**Текущие ограничения:**
- Работает только на Windows (можно адаптировать для Linux/macOS, но не тестировали)
- Нужна Java 21 чтобы собирать моды
- BlueMap карта должна быть доступна по указанному адресу
- Discord бот требует настройки токена в конфигурации

**Текущие недочеты:**
- ⚠️ **WebGL ошибки в консоли** - предупреждения о GPU stall при загрузке BlueMap карты (не критично, не влияет на работу)
- ⚠️ **Discord API rate limits** - при частых запросах аватара бота возможны ограничения (решается кэшированием на 5 минут)
- ⚠️ **Отсутствие автообновлений** - лаунчер не проверяет наличие новых версий автоматически
- ⚠️ **Нет валидации настроек** - пользователь может установить несовместимые параметры RAM
- ⚠️ **Ограниченная обработка ошибок** - некоторые ошибки сети не обрабатываются gracefully
- ⚠️ **Нет поддержки нескольких серверов** - лаунчер настроен только на один сервер
- ⚠️ **Отсутствие темной/светлой темы** - только одна тема интерфейса

**Планируемые улучшения:**
- ✅ Поддержка установки модов через лаунчер (в разработке)
- ✅ Автоматические обновления лаунчера
- ✅ Расширенная статистика игроков
- ✅ Поддержка нескольких серверов
- ✅ Темная/светлая тема интерфейса
- ✅ Улучшенная обработка ошибок
- ✅ Валидация настроек запуска

## Лицензия

MIT License
